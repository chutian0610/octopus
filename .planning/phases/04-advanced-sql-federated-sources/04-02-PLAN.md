---
phase: "04"
plan: "02"
type: "execute"
wave: 2
depends_on: ["04-01"]
files_modified:
  - octopus-executor/Cargo.toml
  - octopus-executor/src/federated_postgres.rs
  - octopus-executor/src/lib.rs
autonomous: true
requirements:
  - "DATA-04"
must_haves:
  truths:
    - "User can register PostgreSQL as a federated datasource"
    - "PostgreSQL queries are executed via connection pool without blocking the worker runtime"
    - "PostgreSQL types are correctly mapped to Arrow types"
  artifacts:
    - path: "octopus-executor/src/federated_postgres.rs"
      provides: "PostgreSQL federated connector implementation"
      exports: ["PostgresFederatedConnector", "PostgresTypeAdapter", "PostgresConnectionPool"]
  key_links:
    - from: "octopus-executor/src/datasource.rs"
      to: "octopus-executor/src/federated_postgres.rs"
      via: "uses PostgresFederatedConnector"
      pattern: "PostgresFederatedConnector"
---

<objective>
Implement PostgreSQL federated connector with connection pooling.

Purpose: Enables Octopus workers to query PostgreSQL databases as federated data sources.
Output: PostgresFederatedConnector implementing FederatedConnector trait.
</objective>

<context>
@.planning/phases/04-advanced-sql-federated-sources/04-01-PLAN.md
@octopus-executor/src/datasource.rs
@octopus-executor/Cargo.toml

## Context Decisions

- **D-01:** Connection pool per worker for PostgreSQL
- **D-04:** Adapter pattern with per-database adapters
</context>

<interfaces>
From 04-01-PLAN.md (plan to create):
```rust
pub trait FederatedConnector: Send + Sync {
    fn database_type(&self) -> DatabaseType;
    fn type_adapter(&self) -> Arc<dyn TypeAdapter>;
    fn connection_pool(&self) -> Arc<dyn ConnectionPool>;
    fn get_schema(&self, table: &str) -> Result<Schema>;
    fn execute_query(&self, sql: &str) -> Result<RecordBatchStream>;
}

pub enum DatabaseType {
    PostgreSQL,
    MySQL,
}
```

From octopus-executor/src/datasource.rs:
```rust
pub struct DataSourceRegistrar {
    // Manages data source registrations
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add PostgreSQL driver dependencies</name>
  <files>octopus-executor/Cargo.toml</files>
  <action>
Add PostgreSQL-related dependencies:
- tokio-postgres = "0.7" — async PostgreSQL driver
- deadpool-postgres = "0.14" — connection pool for PostgreSQL
- postgres-types = "0.2" — PostgreSQL type system
- futures = "0.3" — for Stream implementation

These complement deadpool already in octopus-common.
  </action>
  <verify>cargo build -p octopus-executor 2>&1 | grep -v "^warning" | tail -10</verify>
  <done>octopus-executor builds with PostgreSQL dependencies</done>
</task>

<task type="auto">
  <name>Task 2: Implement PostgresTypeAdapter</name>
  <files>octopus-executor/src/federated_postgres.rs</files>
  <action>
Implement PostgresTypeAdapter in federated_postgres.rs:

1. PostgresTypeAdapter struct implementing TypeAdapter trait (D-04):
   - database_type() returns DatabaseType::PostgreSQL
   - to_arrow_type() maps PostgreSQL types (VARCHAR, INTEGER, BIGINT, FLOAT, DOUBLE PRECISION, BOOLEAN, DATE, TIMESTAMP, UUID, JSON, etc.) to Arrow DataTypes
   - from_arrow_type() reverses the mapping

2. Key PostgreSQL to Arrow mappings:
   - VARCHAR/TEXT -> Utf8
   - INTEGER -> Int32
   - BIGINT -> Int64
   - FLOAT/REAL -> Float32
   - DOUBLE PRECISION -> Float64
   - BOOLEAN -> Boolean
   - DATE -> Date32
   - TIMESTAMP -> Timestamp(TimestampMillisecond, None)
   - UUID -> LargeBinary
   - JSON -> LargeBinary (stored as string)

Import DataType and Schema from datafusion-common.
  </action>
  <verify>cargo build -p octopus-executor 2>&1 | grep -v "^warning" | tail -10</verify>
  <done>PostgresTypeAdapter correctly maps PostgreSQL types to Arrow DataTypes</done>
</task>

<task type="auto">
  <name>Task 3: Implement PostgresConnectionPool</name>
  <files>octopus-executor/src/federated_postgres.rs</files>
  <action>
Implement PostgresConnectionPool in federated_postgres.rs:

1. PostgresConnectionPool struct wrapping deadpool_postgres::Pool:
   - Implements ConnectionPool trait
   - get() returns pooled connection from deadpool
   - release() returns connection to pool (no-op with deadpool)
   - stats() returns PoolStats with available, max, timeout counts

2. Pool configuration:
   - max_size: configurable (default 10 connections)
   - timeout: configurable (default 30 seconds)
   - Create from Config struct with host, port, user, password, database

3. PostgresConnection struct wrapping tokio_postgres::Client for actual queries.

Store pool config in struct fields for reconfiguration.
  </action>
  <verify>cargo build -p octopus-executor 2>&1 | grep -v "^warning" | tail -10</verify>
  <done>PostgresConnectionPool manages PostgreSQL connections via deadpool</done>
</task>

<task type="auto">
  <name>Task 4: Implement PostgresFederatedConnector</name>
  <files>octopus-executor/src/federated_postgres.rs</files>
  <action>
Implement PostgresFederatedConnector in federated_postgres.rs:

1. PostgresFederatedConnector struct containing:
   - pool: Arc<PostgresConnectionPool>
   - type_adapter: PostgresTypeAdapter
   - config: ConnectionConfig (host, port, user, password, database)

2. Implement FederatedConnector trait:
   - database_type() -> DatabaseType::PostgreSQL
   - type_adapter() -> Arc::new(type_adapter) as Arc<dyn TypeAdapter>
   - connection_pool() -> pool as Arc<dyn ConnectionPool>
   - get_schema(table) -> queries information_schema for column types, returns Schema
   - execute_query(sql) -> returns RecordBatchStream from query results

3. For execute_query: use pool.get(), execute simple query, stream results as RecordBatch batches.
   - Use row.get::<_, T>(col_index) with appropriate type conversion
   - Convert to RecordBatch with builder pattern

4. Add constructor: PostgresFederatedConnector::new(config: ConnectionConfig) -> Self

Return errors as OctopusError variants.
  </action>
  <verify>cargo build -p octopus-executor 2>&1 | grep -v "^warning" | tail -10</verify>
  <done>PostgresFederatedConnector fully implements FederatedConnector trait</done>
</task>

<task type="auto">
  <name>Task 5: Export PostgreSQL connector from executor</name>
  <files>octopus-executor/src/lib.rs</files>
  <action>
Update lib.rs to:
1. Add `pub mod federated_postgres;`
2. Export: `pub use federated_postgres::{PostgresFederatedConnector, ConnectionConfig};`

This makes PostgresFederatedConnector available to the coordinator for datasource registration.
  </action>
  <verify>cargo build -p octopus-executor 2>&1 | grep -v "^warning" | tail -10</verify>
  <done>PostgresFederatedConnector exported from octopus-executor</done>
</task>

</tasks>

<verification>
- cargo build -p octopus-executor succeeds
- PostgresFederatedConnector implements FederatedConnector trait fully
- Connection pool created and managed via deadpool
</verification>

<success_criteria>
- DATA-04: "User can query PostgreSQL via federated connector" — verified via working PostgreSQL queries
- Connection pool per worker (D-01) implemented via deadpool
- Type adapter pattern (D-04) implemented via PostgresTypeAdapter
</success_criteria>

<output>
After completion, create `.planning/phases/04-advanced-sql-federated-sources/04-02-SUMMARY.md`
</output>