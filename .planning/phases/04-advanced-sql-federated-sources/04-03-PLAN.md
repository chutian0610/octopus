---
phase: "04"
plan: "03"
type: "execute"
wave: 2
depends_on: ["04-01"]
files_modified:
  - octopus-executor/Cargo.toml
  - octopus-executor/src/federated_mysql.rs
  - octopus-executor/src/lib.rs
autonomous: true
requirements:
  - "DATA-05"
must_haves:
  truths:
    - "User can register MySQL as a federated datasource"
    - "MySQL queries are executed via connection pool without blocking the worker runtime"
    - "MySQL types are correctly mapped to Arrow types"
  artifacts:
    - path: "octopus-executor/src/federated_mysql.rs"
      provides: "MySQL federated connector implementation"
      exports: ["MysqlFederatedConnector", "MysqlTypeAdapter", "MysqlConnectionPool"]
  key_links:
    - from: "octopus-executor/src/datasource.rs"
      to: "octopus-executor/src/federated_mysql.rs"
      via: "uses MysqlFederatedConnector"
      pattern: "MysqlFederatedConnector"
---

<objective>
Implement MySQL federated connector with connection pooling.

Purpose: Enables Octopus workers to query MySQL databases as federated data sources.
Output: MysqlFederatedConnector implementing FederatedConnector trait.
</objective>

<context>
@.planning/phases/04-advanced-sql-federated-sources/04-01-PLAN.md
@.planning/phases/04-advanced-sql-federated-sources/04-02-PLAN.md
@octopus-executor/Cargo.toml

## Context Decisions

- **D-01:** Connection pool per worker for MySQL
- **D-04:** Adapter pattern with per-database adapters
</context>

<interfaces>
From 04-01-PLAN.md (plan to create):
```rust
pub trait FederatedConnector: Send + Sync {
    fn database_type(&self) -> DatabaseType;
    fn type_adapter(&self) -> Arc<dyn TypeAdapter>;
    fn connection_pool(&self) -> Arc<dyn ConnectionPool>;
    fn get_schema(&self, table: &str) -> Result<Schema>;
    fn execute_query(&self, sql: &str) -> Result<RecordBatchStream>;
}

pub enum DatabaseType {
    PostgreSQL,
    MySQL,
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add MySQL driver dependencies</name>
  <files>octopus-executor/Cargo.toml</files>
  <action>
Add MySQL-related dependencies:
- mysql_async = "0.34" — async MySQL driver
- deadpool-mysql = "0.14" — connection pool for MySQL
- mysql-types = "0.31" — MySQL type system

These complement deadpool already in octopus-common.
  </action>
  <verify>cargo build -p octopus-executor 2>&1 | grep -v "^warning" | tail -10</verify>
  <done>octopus-executor builds with MySQL dependencies</done>
</task>

<task type="auto">
  <name>Task 2: Implement MysqlTypeAdapter</name>
  <files>octopus-executor/src/federated_mysql.rs</files>
  <action>
Implement MysqlTypeAdapter in federated_mysql.rs:

1. MysqlTypeAdapter struct implementing TypeAdapter trait (D-04):
   - database_type() returns DatabaseType::MySQL
   - to_arrow_type() maps MySQL types (VARCHAR, CHAR, INT, INTEGER, BIGINT, FLOAT, DOUBLE, BIT, DATE, DATETIME, TIMESTAMP, JSON, etc.) to Arrow DataTypes
   - from_arrow_type() reverses the mapping

2. Key MySQL to Arrow mappings:
   - VARCHAR/CHAR/TEXT -> Utf8
   - INT/INTEGER -> Int32
   - BIGINT -> Int64
   - FLOAT -> Float32
   - DOUBLE -> Float64
   - BIT/BOOL -> Boolean
   - DATE -> Date32
   - DATETIME/TIMESTAMP -> Timestamp(TimestampMillisecond, None)
   - JSON -> LargeBinary (stored as string)
   - BINARY/BLOB -> LargeBinary

Import DataType and Schema from datafusion-common.
  </action>
  <verify>cargo build -p octopus-executor 2>&1 | grep -v "^warning" | tail -10</verify>
  <done>MysqlTypeAdapter correctly maps MySQL types to Arrow DataTypes</done>
</task>

<task type="auto">
  <name>Task 3: Implement MysqlConnectionPool</name>
  <files>octopus-executor/src/federated_mysql.rs</files>
  <action>
Implement MysqlConnectionPool in federated_mysql.rs:

1. MysqlConnectionPool struct wrapping deadpool_mysql::Pool:
   - Implements ConnectionPool trait
   - get() returns pooled connection from deadpool
   - release() returns connection to pool (no-op with deadpool)
   - stats() returns PoolStats with available, max, timeout counts

2. Pool configuration:
   - max_size: configurable (default 10 connections)
   - timeout: configurable (default 30 seconds)
   - Create from Config struct with host, port, user, password, database

3. MysqlConnection struct wrapping mysql_async::Conn for actual queries.

Store pool config in struct fields for reconfiguration.
  </action>
  <verify>cargo build -p octopus-executor 2>&1 | grep -v "^warning" | tail -10</verify>
  <done>MysqlConnectionPool manages MySQL connections via deadpool</done>
</task>

<task type="auto">
  <name>Task 4: Implement MysqlFederatedConnector</name>
  <files>octopus-executor/src/federated_mysql.rs</files>
  <action>
Implement MysqlFederatedConnector in federated_mysql.rs:

1. MysqlFederatedConnector struct containing:
   - pool: Arc<MysqlConnectionPool>
   - type_adapter: MysqlTypeAdapter
   - config: ConnectionConfig (host, port, user, password, database)

2. Implement FederatedConnector trait:
   - database_type() -> DatabaseType::MySQL
   - type_adapter() -> Arc::new(type_adapter) as Arc<dyn TypeAdapter>
   - connection_pool() -> pool as Arc<dyn ConnectionPool>
   - get_schema(table) -> queries information_schema for column types, returns Schema
   - execute_query(sql) -> returns RecordBatchStream from query results

3. For execute_query: use pool.get(), execute query, stream results as RecordBatch batches.
   - Use row.get::<_, T>(col_index) with appropriate type conversion
   - Convert to RecordBatch with builder pattern

4. Add constructor: MysqlFederatedConnector::new(config: ConnectionConfig) -> Self

Return errors as OctopusError variants.
  </action>
  <verify>cargo build -p octopus-executor 2>&1 | grep -v "^warning" | tail -10</verify>
  <done>MysqlFederatedConnector fully implements FederatedConnector trait</done>
</task>

<task type="auto">
  <name>Task 5: Export MySQL connector from executor</name>
  <files>octopus-executor/src/lib.rs</files>
  <action>
Update lib.rs to:
1. Add `pub mod federated_mysql;`
2. Export: `pub use federated_mysql::{MysqlFederatedConnector, ConnectionConfig};`

This makes MysqlFederatedConnector available to the coordinator for datasource registration.
  </action>
  <verify>cargo build -p octopus-executor 2>&1 | grep -v "^warning" | tail -10</verify>
  <done>MysqlFederatedConnector exported from octopus-executor</done>
</task>

</tasks>

<verification>
- cargo build -p octopus-executor succeeds
- MysqlFederatedConnector implements FederatedConnector trait fully
- Connection pool created and managed via deadpool
</verification>

<success_criteria>
- DATA-05: "User can query MySQL via federated connector" — verified via working MySQL queries
- Connection pool per worker (D-01) implemented via deadpool
- Type adapter pattern (D-04) implemented via MysqlTypeAdapter
</success_criteria>

<output>
After completion, create `.planning/phases/04-advanced-sql-federated-sources/04-03-SUMMARY.md`
</output>