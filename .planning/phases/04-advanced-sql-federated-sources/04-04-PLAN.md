---
phase: "04"
plan: "04"
type: "execute"
wave: 3
depends_on: ["04-01"]
files_modified:
  - octopus-coordinator/Cargo.toml
  - octopus-coordinator/src/query_service.rs
  - octopus-coordinator/src/stage_planner.rs
autonomous: true
requirements:
  - "ADV-01"
  - "ADV-02"
  - "ADV-03"
must_haves:
  truths:
    - "User can use window functions (ROW_NUMBER, RANK, DENSE_RANK, LEAD, LAG) in queries"
    - "User can use date/time functions (date_trunc, EXTRACT, date_diff) and string functions (SUBSTR, CONCAT, REGEXP)"
    - "User can use type conversions (CAST, TRY_CAST) and CASE/COALESCE/NVL expressions"
  artifacts:
    - path: "octopus-coordinator/src/query_service.rs"
      provides: "Updated query service with advanced SQL support"
      contains: "window functions, date/time, string, type conversion"
  key_links:
    - from: "octopus-coordinator/src/stage_planner.rs"
      to: "octopus-coordinator/src/query_service.rs"
      via: "uses advanced SQL functions in query planning"
      pattern: "Window|window|Rank|RANK|Lead|LEAD"
---

<objective>
Enable advanced SQL features: window functions, date/time/string functions, type conversions.

Purpose: Full SQL feature coverage for complex analytical queries (D-02, D-03).
Output: Coordinator handles advanced SQL features via DataFusion's built-in implementations.
</objective>

<context>
@.planning/phases/04-advanced-sql-federated-sources/04-CONTEXT.md
@octopus-coordinator/src/query_service.rs
@octopus-coordinator/src/stage_planner.rs
@.planning/STATE.md

## Context Decisions

- **D-02:** Window functions — Auto mode (DataFusion decides streaming vs bounded)
- **D-03:** Function coverage — Built-in + custom for performance-critical functions
</context>

<interfaces>
From octopus-coordinator/src/query_service.rs:
```rust
pub struct QueryService {
    // Query execution service
}

impl QueryService {
    pub async fn execute_query(&self, query: &str, context: SessionContext) -> Result<DataFrame> {
        // Uses SessionContext::sql() for SQL parsing
    }
}
```

From octopus-coordinator/src/stage_planner.rs:
```rust
pub struct StagePlanner {
    // Plans stage DAG from LogicalPlan
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Verify DataFusion built-in window functions</name>
  <files>octopus-coordinator/src/query_service.rs</files>
  <action>
Check that DataFusion 43 properly supports window functions in query planning.

1. Read current query_service.rs to understand the query execution flow
2. Verify that LogicalPlan from SessionContext::sql() includes window functions
3. Test with sample window function query:
   - ROW_NUMBER() OVER (PARTITION BY col ORDER BY col)
   - RANK() OVER (PARTITION BY col ORDER BY col)  
   - DENSE_RANK() OVER (PARTITION BY col ORDER BY col)
   - LEAD(col) OVER (PARTITION BY col ORDER BY col)
   - LAG(col) OVER (PARTITION BY col ORDER BY col)

4. Window frames: ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW

DataFusion's SQL planner parses these automatically. The coordinator just needs to:
- Pass queries through without modification
- Ensure Exchange operators correctly handle window function partitioning

Add documentation comment to query_service.rs explaining window function support:
```rust
// Advanced SQL Features (D-02, D-03)
// Window functions (ROW_NUMBER, RANK, DENSE_RANK, LEAD, LAG) are parsed
// by DataFusion's SQL planner and executed with auto mode (streaming vs bounded).
// Date/time functions (date_trunc, EXTRACT, date_diff), string functions
// (SUBSTR, CONCAT, REGEXP_MATCH), and type conversions (CAST, TRY_CAST, CASE)
// are supported via DataFusion built-ins.
```
  </action>
  <verify>cargo build -p octopus-coordinator 2>&1 | grep -v "^warning" | tail -10</verify>
  <done>Window function support verified in query_service.rs</done>
</task>

<task type="auto">
  <name>Task 2: Verify DataFusion built-in date/time and string functions</name>
  <files>octopus-coordinator/src/query_service.rs</files>
  <action>
Verify and document DataFusion built-in functions for advanced SQL:

1. **Date/Time functions (ADV-02):**
   - date_trunc('interval', timestamp) — truncate to interval
   - EXTRACT(field FROM timestamp) — extract year, month, day, hour, etc.
   - date_diff('interval', start, end) — difference between dates
   
2. **String functions (ADV-02):**
   - substr(string, start, length) — substring extraction
   - concat(string, ...) — string concatenation  
   - regexp_match(string, pattern) — regex matching
   
3. **Type conversion functions (ADV-03):**
   - CAST(expression AS type) — explicit type conversion
   - TRY_CAST(expression AS type) — null on conversion failure
   - CASE WHEN ... THEN ... END — conditional expression
   - COALESCE(value, ...) — first non-null value
   - NVL(value1, value2) — alias for COALESCE

These are all in DataFusion's built-in function registry. No custom implementations needed unless performance issues identified (D-03).

Add verification test: execute a query using all three categories and verify no errors.
  </action>
  <verify>cargo build -p octopus-coordinator 2>&1 | grep -v "^warning" | tail -10</verify>
  <done>Date/time, string, and type conversion functions verified via DataFusion built-ins</done>
</task>

<task type="auto">
  <name>Task 3: Ensure advanced functions work through Exchange operators</name>
  <files>octopus-coordinator/src/stage_planner.rs</files>
  <action>
Ensure advanced SQL features integrate correctly with Exchange operators in stage planning.

1. Read stage_planner.rs to understand how LogicalPlan is converted to stage DAG
2. Verify that window functions and other advanced constructs are handled:
   - Window function requires partitioning — check that partition keys are preserved
   - Aggregate functions with window frames — ensure correct grouping
   - Complex expressions in SELECT — ensure evaluation order

3. Key concern: Window functions create boundaries in streaming (each partition processed independently). The stage planner should:
   - Keep window operations in a single stage (not split by Exchange)
   - Handle partition ordering correctly

4. Add planning rules if needed:
   - WindowAgg node stays together (no exchange above it)
   - ORDER BY in window specification preserved through planning

If issues found, implement fixes in stage_planner.rs.
  </action>
  <verify>cargo build -p octopus-coordinator 2>&1 | grep -v "^warning" | tail -10</verify>
  <done>Advanced SQL features correctly integrated through Exchange operators</done>
</task>

</tasks>

<verification>
- cargo build -p octopus-coordinator succeeds
- cargo test -p octopus-coordinator passes
- Window function queries execute successfully
- Date/time, string, type conversion queries execute successfully
</verification>

<success_criteria>
- ADV-01: Window functions (ROW_NUMBER, RANK, DENSE_RANK, LEAD, LAG) with frames work
- ADV-02: date_trunc, EXTRACT, date_diff, SUBSTR, CONCAT, REGEXP work
- ADV-03: CAST, TRY_CAST, CASE, COALESCE, NVL work
- Auto mode (D-02): DataFusion decides streaming vs bounded
- Built-in functions (D-03): Using DataFusion implementations
</success_criteria>

<output>
After completion, create `.planning/phases/04-advanced-sql-federated-sources/04-04-SUMMARY.md`
</output>